node_modules
dist
.DS_Store

# Supabase CLI's local project-link cache (created by `supabase login`/
# `functions deploy`) — machine-local, not shared state.
supabase/.temp/
*.local
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Generated locally by scripts/export-feedback.ts — contains your email
# address (whoever's logged in when they submit feedback), so it stays out
# of the public repo. Regenerate any time; I can read it directly during
# sessions since it's still on disk.
FEEDBACK.md

# Source docs Andrew drops in docs/knowledge/ for reference — not meant to be
# tracked (docs/knowledge/puppy-life-knowledge.md, which is derived from
# these, stays tracked).
docs/knowledge/*.docx
docs/knowledge/*.xlsx
